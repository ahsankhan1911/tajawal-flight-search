      // if (search.length <= 2) {
        //    return next("Please provide atleast 3 letters to search");
        // } 
    
            // Flight.find({'name' : {$regex: search, $options: 'i'}}, (err, flights) => {
            

            //     if(err) {
            //         res.send(err);
            //     }

            //     else {
            //         res.send(flights)
            //     }
            // })
    
                // .then((doc) => {
                //     if (doc.length === 0) {
                //         return next(boom.badImplementation("no such airport with keyword " + search + " found"));
                //     }
                    
                //     res.send(doc);
                   
                // })
                // .catch((err) => {
    
                //     next(boom.badImplementation(err.toString()));
    
                // });